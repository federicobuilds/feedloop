"""Frozen offline evaluation over the public ranking sources. Experimental: it is not part
of the installed package, it pins ``src/feedloop/{ranking,taste,profiles}.py`` by hash, and
it never replaces an unmeasured outcome with a score. See README.md in this directory."""
